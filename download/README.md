Here are all the generated files.

## SQL migrations (run in Supabase SQL Editor)

1. `schema.sql` — full schema (fresh projects)
2. `add-whatsapp-column.sql` — WhatsApp field
3. `add-price-column.sql` — catalog price/unit
4. `add-location-columns.sql` — **latitude / longitude for map & nearby search** (required for location feature)
5. `add-request-location-columns.sql` — موقع طلب الخدمة (إحداثيات + عنوان مقروء)

